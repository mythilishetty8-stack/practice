go into your directory and make files
git config both name and email
git init
git add .
git commit -m ""
git remote add origin https://github.com/mythilishetty8-stack/practice
git pull origin main --allow-unrelated-histories
git push origin main
